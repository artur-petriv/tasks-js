/*
Написать код, который из массива сделает обьект.
*/

// Моё решение 1
function transform(arr) {
  let obj = {
    name: "",
    age: "",
  };

  arr.forEach((elem) => {});
}

const arr = [
  { name: "name", value: "Анатолий" },
  { name: "age", value: 40 },
];

console.log(transform(arr)); // {name: 'Анатолій', age: 40}
